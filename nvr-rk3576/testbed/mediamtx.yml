# Minimal config for the M1 local test harness: 4 independent paths,
# one per simulated camera. All server defaults apply (RTSP :8554,
# RTMP :1935, HLS :8888).
hlsAlwaysRemux: true
# MPEG-TS (original). fMP4 was tried but the muxer crash is load/UDP-driven,
# not variant-driven (isolated mpegts is stable, live fmp4 crashes) — the fix
# is -rtsp_transport tcp on the publishers (see start_testbed.sh).
hlsVariant: mpegts
# 500ms segments: 1s segments limited cam_c's preview content updates to
# ~1/s (slideshow-like); 200ms broke the remux of the lossy RTSP feed.
hlsSegmentDuration: 500ms

paths:
  cam_a:
  cam_b:
  cam_c:
  cam_d:
